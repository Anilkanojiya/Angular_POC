import { AppDisplayDirective } from './app-display.directive';

describe('AppDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
